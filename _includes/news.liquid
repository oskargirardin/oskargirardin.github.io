<div class="news">
  {% if site.news != blank %}
    {% assign news_size = site.news | size %}
    <div
      class="table-responsive"
      {% if include.limit and page.announcements.scrollable and news_size > 3 %}
        style="max-height: 60vw"
      {% endif %}
    >
      <table class="table table-sm table-borderless">
        {% assign news = site.news | reverse %}
        {% if include.limit and page.announcements.limit %}
          {% assign news_limit = page.announcements.limit %}
        {% else %}
          {% assign news_limit = news_size %}
        {% endif %}
        {% for item in news limit: news_limit %}
          <tr>
            <th scope="row" style="width: 20%">{{ item.date | date: '%b %d, %Y' }}</th>
            <td>
            {% if item.inline and item.image%}

                <table class="table table-sm table-borderless">
                    <tr>
                        <td >
                            {{ item.content | remove: '<p>' | remove: '</p>' | emojify }}
                        </td>
                        <td style="width: 20%">
                          {% assign image_path = item.image | prepend: 'assets/img/' %}
                            {% assign image_class = 'img-fluid z-depth-1 rounded' %}
                          {% capture sizes %}{{ site.max_width | minus: 30 | times: 0.8}}px {% endcapture %}
                          {%
                            include figure.liquid loading="eager" path=image_path class=image_class sizes=sizes alt=item.image
                            cache_bust=true zoomable=true
                          %}
                        </td>
                    </tr>
                  </table>
              {% elsif item.inline %}
                {{ item.content | remove: '<p>' | remove: '</p>' | emojify }}
              {% else %}
                <a class="news-title" href="{{ item.url | relative_url }}">{{ item.title }}</a>
              {% endif %}
            </td>
          </tr>
        {% endfor %}
      </table>
    </div>
  {% else %}
    <p>No news so far...</p>
  {% endif %}
</div>
